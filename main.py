import star_voting_systems
from star_voting_systems import Ballot
from random import randint

"""
Using random.randint to create a random int
Need to sudo code how I want to control a ballot's 
id so I don't submit duplicates and poll id's so I 
can associate multiple ballots
"""

current_ballots = ()


def create_random_ballot(amount, ballot_id):
    for x in range(0, amount):


    return


def main():
    create_random_ballot(3, 'abc')
    return


# Press the green button in the gutter to run the script.
if __name__ == '__main__':
    main()
